# Character Assets

Place your approved web-viewable 3D character here.

Recommended primary preview asset:

- `main_character.glb`

Optional source-production assets:

- `main_character.blend`
- `main_character.fbx`

The Gradio **Character Viewer** tab will try to load `models/characters/main_character.glb` by default.
